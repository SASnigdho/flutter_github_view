import 'package:flutter/material.dart';

class SavedRepositoryTab extends StatelessWidget {
  const SavedRepositoryTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Saved RepositoryTab');
  }
}
